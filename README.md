# House-Price-Predictor
A House Predictor system made using regression machine learning model, that predicts the potential selling price of an house, apartment or a flat based on it's features i.e parameters for the ML model. Currently only select cities from Maharashtra are added in database.
